import React from 'react';

const Navbar = ({ title }) => {
  return (
    <nav>
      <h1>{title}</h1>
      {/* Agrega más elementos de navegación si es necesario */}
    </nav>
  );
}

export default Navbar;